## Level 11

For this level, we will use the ```base64``` command that encodes and decodes, with the ```-d``` flag, in the base64 alphabet.

Steps:

```base64 -d data.txt```

After doing so, the password is:

<details>
  <summary>Click me for Password</summary>
  pYfOY6HwUsDj5rL9UvyhU7MCmv8vN5Ro
  
</details>
