## Level 1

Make sure you are logged into the Bandit server with the ```ssh bandit0@bandit.labs.overthewire.org -p 2220``` command mentioned in level 0.

I used ```ls```, the list directory command, to show the directory contents.  This showed a single readme file.  To read it, I used ```cat <file>```, the concatenate command, which reads the input to the terminal.  In this specific instance, the correct command would be ```cat readme``` to get the password.

After doing so, the password is:

<details>
  <summary>Click me for Password</summary>
  6y2kwnwK6grgvwvpvLaa2T1cpFEKOhNR
  
</details>
